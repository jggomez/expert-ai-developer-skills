# Architecture & Key Decisions

<!-- shared-context:rollup -->
_Accumulated from each session's `decisions.md` by `context_rollup.py`. The
newest section is appended at the end. Mark superseded decisions with
`~~strikethrough~~` plus a short note rather than deleting them, so the history
of why the design changed stays readable._
