# Decisions — {{SESSION}}

_One bullet per decision that has a real trade-off or that another agent must
respect. Skip trivial or purely local choices. Format:_
**decision** — _why, and what was rejected._

_These lines are rolled up verbatim into `context/architecture.md`, so make each
one self-contained._

- _(example) **Store context as `tar.xz`, not zip** — stdlib-only and a better ratio for text; zip rejected for weaker compression._
